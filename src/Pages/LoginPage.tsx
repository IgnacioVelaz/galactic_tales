import { LoginForm } from "../components/LoginForm/LoginForm"

export const LoginPage  = ()=> {
  return (
    <div className="h-screen bg-bgPrimary flex justify-center items-center">
    <LoginForm />
    </div>
  )
}